name: Publish Github

on:
  workflow_call:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Update Version
        uses: ./.github/workflows/verson.yml@main
      - name: Download Artifact
        uses: actions/download-artifact@v3
        with:
          name: cosmoflex-build
      - run: ls
      # - run: yarn publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{secrets.npm_token}}
